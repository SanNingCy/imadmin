<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seekweb4.chat.modules.piamom.mapper.PiamomSquarePublishQuotaMapper">

    <resultMap id="BaseResultMap" type="com.seekweb4.chat.modules.piamom.entity.PiamomSquarePublishQuota">
        <id column="id" property="id"/>
        <result column="credit_min" property="creditMin"/>
        <result column="credit_max" property="creditMax"/>
        <result column="daily_limit" property="dailyLimit"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, credit_min, credit_max, daily_limit, sort_order, status, created_at, updated_at
    </sql>

    <select id="selectAll" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
        from t_piamom_square_publish_quota
        order by sort_order desc, credit_min asc
    </select>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        select <include refid="Base_Column_List"/>
        from t_piamom_square_publish_quota
        where id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into t_piamom_square_publish_quota (credit_min, credit_max, daily_limit, sort_order, status)
        values (#{creditMin}, #{creditMax}, #{dailyLimit}, #{sortOrder}, #{status})
    </insert>

    <update id="updateByPrimaryKeySelective">
        update t_piamom_square_publish_quota
        <set>
            <if test="creditMin != null">credit_min = #{creditMin},</if>
            <if test="creditMax != null">credit_max = #{creditMax},</if>
            <if test="dailyLimit != null">daily_limit = #{dailyLimit},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
            updated_at = now()
        </set>
        where id = #{id}
    </update>

    <delete id="deleteByPrimaryKey">delete from t_piamom_square_publish_quota where id = #{id}</delete>
</mapper>
