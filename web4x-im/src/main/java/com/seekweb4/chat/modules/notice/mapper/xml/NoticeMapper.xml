<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seekweb4.chat.modules.notice.mapper.NoticeMapper">

	<sql id="noticeColumns">
		a.id AS "id",
		a.info AS "info",
		a.create_date AS "createDate",
		a.update_date AS "updateDate"
	</sql>

	<sql id="noticeJoins">

	</sql>


	<select id="get" resultType="com.seekweb4.chat.modules.notice.entity.Notice" >
		SELECT
			<include refid="noticeColumns"/>
		FROM t_notice a
		<include refid="noticeJoins"/>
		WHERE a.id = #{id}
	</select>

	<select id="findList" resultType="com.seekweb4.chat.modules.notice.entity.Notice" >
		SELECT
			<include refid="noticeColumns"/>
		FROM t_notice a
		<include refid="noticeJoins"/>
		<where>
			
			${dataScope}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.create_date DESC
			</otherwise>
		</choose>
	</select>

	<select id="findAllList" resultType="com.seekweb4.chat.modules.notice.entity.Notice" >
		SELECT
			<include refid="noticeColumns"/>
		FROM t_notice a
		<include refid="noticeJoins"/>
		<where>
			
			${dataScope}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>

	<insert id="insert">
		INSERT INTO t_notice(
			id,
			info,
			create_date,
			update_date
		) VALUES (
			#{id},
			#{info},
			#{createDate},
			#{updateDate}
		)
	</insert>

	<update id="update">
		UPDATE t_notice SET
			info = #{info},
			update_date = #{updateDate}
		WHERE id = #{id}
	</update>


	<!--物理删除-->
	<update id="delete">
		DELETE FROM t_notice
		WHERE id = #{id}
	</update>

	<!--逻辑删除-->
	<update id="deleteByLogic">
		UPDATE t_notice SET
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>


	<!-- 根据实体名称和字段名称和字段值获取唯一记录 -->
	<select id="findUniqueByProperty" resultType="com.seekweb4.chat.modules.notice.entity.Notice">
		SELECT
			<include refid="noticeColumns"/>
		FROM t_notice a
		<include refid="noticeJoins"/>
		WHERE a.${propertyName} = #{value}
	</select>

</mapper>