<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.seekweb4.chat.modules.live.mapper.LiveBillingRuleMapper">

    <resultMap id="BaseResultMap" type="com.seekweb4.chat.modules.live.entity.LiveBillingRule">
        <id column="id" property="id"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="rounding_rule" property="roundingRule"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, unit_price, rounding_rule, status, remark, create_by, update_by, create_time, update_time, is_deleted
    </sql>

    <select id="selectActiveOne" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_live_billing_rule
        where is_deleted = 0 and status = 1
        order by update_time desc, id desc
        limit 1
    </select>

    <select id="selectAdminPageList" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_live_billing_rule
        <where>
            is_deleted = 0
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="unitPrice != null">
                and unit_price = #{unitPrice}
            </if>
            <if test="createBy != null and createBy != ''">
                and create_by = #{createBy}
            </if>
            <if test="updateBy != null and updateBy != ''">
                and update_by = #{updateBy}
            </if>
        </where>
        <if test="orderBy != null and orderBy != ''">
            order by ${orderBy}
        </if>
        <if test="orderBy == null or orderBy == ''">
            order by create_time desc
        </if>
        <if test="pageNo != null and pageSize != null">
            limit #{pageNo}, #{pageSize}
        </if>
    </select>

    <select id="selectAdminCount" resultType="java.lang.Long">
        select count(1)
        from t_live_billing_rule
        <where>
            is_deleted = 0
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="unitPrice != null">
                and unit_price = #{unitPrice}
            </if>
            <if test="createBy != null and createBy != ''">
                and create_by = #{createBy}
            </if>
            <if test="updateBy != null and updateBy != ''">
                and update_by = #{updateBy}
            </if>
        </where>
    </select>

    <select id="selectByPrimaryKey" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_live_billing_rule
        where id = #{id} and is_deleted = 0
    </select>

    <update id="updateByPrimaryKeySelective" parameterType="com.seekweb4.chat.modules.live.entity.LiveBillingRule">
        update t_live_billing_rule
        <set>
            <if test="unitPrice != null">
                unit_price = #{unitPrice},
            </if>
            <if test="renewalRules != null">
                rounding_rule = #{renewalRules},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
            <if test="createBy != null">
                create_by = #{createBy},
            </if>
            <if test="updateBy != null">
                update_by = #{updateBy},
            </if>
            <if test="isDeleted != null">
                is_deleted = #{isDeleted},
            </if>
            update_time = now()
        </set>
        where id = #{id}
    </update>

    <insert id="insert" parameterType="com.seekweb4.chat.modules.live.entity.LiveBillingRule" useGeneratedKeys="true" keyProperty="id">
        insert into t_live_billing_rule (unit_price, rounding_rule, status, remark, create_by, update_by, create_time, update_time, is_deleted)
        values (#{unitPrice}, #{roundingRule}, #{status}, #{remark}, #{createBy}, #{updateBy}, now(), now(), #{isDeleted})
    </insert>

</mapper>

